# Commits that changed formatting only. `git blame` should attribute the lines
# they touched to whoever last changed the code, not to the reformat.
#
# Enabled per clone by `npm run hooks:install`; GitHub reads this file itself.

# Format the tree with Biome, and make the formatter part of the gate
406e78371bccaf79c0c898613bd181657e652a9e
