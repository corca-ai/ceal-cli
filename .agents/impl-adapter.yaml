version: 1
repo: ceal-cli
language: en
output_dir: charness-artifacts/impl
preset_id: portable-defaults
customized_from: portable-defaults
verification_tools:
  - "cmd:npm"
ui_verification_tools:
  - "cmd:npx"
verification_install_proposals:
  - "Run `npx playwright install chromium` after dependency installation before the browser proof."
truth_surfaces:
  - README.md
  - worker-release-inputs.json
  - gateway-protocol-handoff-lock.json
  - protocol-vendor-pin.json
