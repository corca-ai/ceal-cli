# ceal-v0.76.0 Release Record

## Status

Candidate. No tag, published worker artifact, installed update, macOS install, or
live Gateway/provider readback is claimed yet.

## Intended Value

- Keep static CLI routes off the full runtime and expose fixed, secret-free
  client timing phases with `ceal --timing` on stderr while preserving stdout.
- Make local state, receipt, guide, and session behavior race-safe and make
  partial or failed work visible to CLI users.
- Consume the exact signed Gateway Protocol handoff v0.72.13 and bind both
  Protocol and packed client provenance into the worker release inventory.

## Immutable Gateway Input

- Tag: `gateway-protocol-handoff-v0.72.13`
- Gateway commit: `b644bdcc1883a12f30dec9f15a918eca3676b740`
- Protocol tree: `07c79e9626a1ef0be3c3a3092ebff48a4433d70d`
- Archive SHA-256:
  `c7c410b48eb65c0a463d0e86090e8a86a67ab8a4ef3eb6a0d12ff7aee05ef7ec`
- Gateway Actions run: `31324956418`

The re-checking homes are `gateway-protocol-handoff-lock.json`,
`protocol-vendor-pin.json`, and `node scripts/verify-protocol-vendor-pin.mjs`.

## Candidate Proof

- The handoff bootstrap verified checksums, the exact Sigstore workflow identity
  and issuer, certificate workflow SHA, remote tag commit, archive inventory,
  signed packet, and Protocol provenance before consumption.
- The exact vendored Protocol suite passes through `npm --ignore-scripts
  --prefix packages/ceal-protocol test`.
- The committed frozen tree matches its pin through `node
  scripts/verify-protocol-vendor-pin.mjs`.
- The final candidate passes `npm run check`, `npm run check:duplication`,
  `npm run lint:shell`, `node scripts/install-git-hooks.mjs --check`, and `git
  diff --check` on the release host.
- Exact source commit `f86de98fac4d3f629f1c74ba64785d5da124032a`
  passes GitHub run `31328838700`: macOS job `93283655563` and Linux job
  `93283655579` both completed the full gate. This includes the repaired Darwin
  local-store lock and its host-only fail-closed regression.
- Final proof still required: release dispatch dry run, tag workflow, public
  artifact verification, and installed Linux readback.

## Proof Ceiling

This record will not promote Linux local or released-binary evidence into a
macOS install claim. It will not promote static installed readbacks into a live
Gateway session or provider-effect claim.
