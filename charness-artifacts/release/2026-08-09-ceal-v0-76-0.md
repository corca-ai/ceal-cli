# ceal-v0.76.0 Release Record

## Status

Released. Annotated tag `ceal-v0.76.0` points to
`df7378c03ff078073f6870707e7908f5229c81b2`; release run `31329394337`
published and activated the signed stable worker set. The installed Linux
worker was updated from `0.75.0` to `0.76.0` and read back locally. No macOS
install or live Gateway/provider readback is claimed.

## Intended Value

- Keep static CLI routes off the full runtime and expose fixed, secret-free
  client timing phases with `ceal --timing` on stderr while preserving stdout.
- Make local state, receipt, guide, and session behavior race-safe and make
  partial or failed work visible to CLI users.
- Consume the exact signed Gateway Protocol handoff v0.72.13 and bind both
  Protocol and packed client provenance into the worker release inventory.

## Immutable Gateway Input

- Tag: `gateway-protocol-handoff-v0.72.13`
- Gateway commit: `b644bdcc1883a12f30dec9f15a918eca3676b740`
- Protocol tree: `07c79e9626a1ef0be3c3a3092ebff48a4433d70d`
- Archive SHA-256:
  `c7c410b48eb65c0a463d0e86090e8a86a67ab8a4ef3eb6a0d12ff7aee05ef7ec`
- Gateway Actions run: `31324956418`

The re-checking homes are `gateway-protocol-handoff-lock.json`,
`protocol-vendor-pin.json`, and `node scripts/verify-protocol-vendor-pin.mjs`.

## Release Proof

- The handoff bootstrap verified checksums, the exact Sigstore workflow identity
  and issuer, certificate workflow SHA, remote tag commit, archive inventory,
  signed packet, and Protocol provenance before consumption.
- The exact vendored Protocol suite passes through `npm --ignore-scripts
  --prefix packages/ceal-protocol test`.
- The committed frozen tree matches its pin through `node
  scripts/verify-protocol-vendor-pin.mjs`.
- The final candidate passes `npm run check`, `npm run check:duplication`,
  `npm run lint:shell`, `node scripts/install-git-hooks.mjs --check`, and `git
  diff --check` on the release host.
- Exact source commit `f86de98fac4d3f629f1c74ba64785d5da124032a`
  passes GitHub run `31328838700`: macOS job `93283655563` and Linux job
  `93283655579` both completed the full gate. This includes the repaired Darwin
  local-store lock and its host-only fail-closed regression.
- Exact tag commit `df7378c03ff078073f6870707e7908f5229c81b2` passed release
  dry run `31329067419`; its signing and publication job was skipped as designed.
- Tag run `31329394337` completed every build, assembly, signing, publication,
  and stable-pointer job. The public `SHA256SUMS` digest is
  `a2d2cbaf7173c67777c30eb371b066d3cac8d310f239ac916aaf774d84cd5757`.
- A fresh public download passed `sha256sum -c SHA256SUMS`. Every primary asset
  passed Cosign verification against repository `corca-ai/ceal-cli`, ref
  `refs/tags/ceal-v0.76.0`, workflow `.github/workflows/ceal-release.yml`, and
  the GitHub Actions OIDC issuer. The stable pointer named this tag and digest,
  and the stable installer byte-matched the versioned installer.
- With no legacy `ceal` process running, `ceal update` installed `0.76.0` on
  Linux ARM64. `ceal version`, `ceal commands`, `ceal --timing version`, and the
  real installed `ceal guide status` read back the released binary, declared
  routes, stderr-only timing stream, and staged signed guide. The guide status
  was `available`, `update_safe`, and registered for the detected Codex host.

## Proof Ceiling

Hosted macOS proves source, local-store behavior, packaging, and the Darwin
artifact build; it does not prove installation on an operator's Mac. Linux
installed readbacks do not prove a live Gateway session, capability execution,
provider effect, receipt, or Gateway audit custody.
