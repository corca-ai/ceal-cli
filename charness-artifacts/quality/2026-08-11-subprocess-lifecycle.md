# Quality Review
Date: 2026-08-11
Title: Subprocess lifecycle and operator entrypoints

## Scope

Target boundaries: sanctioned probes, subprocess inventory, guide release/install carrier, audit skill, and agent entrypoint. Protocol, sibling, and issue state were read-only.

Ambient: B1 proof/shipment divergence remains; no signed/installed proof is claimed.

## Surface Contract Review

- semantic coverage: `partial` — local settlement/guide contracts, not signed install or live behavior.
- surface: `ceal commands`, `ceal observe --help`, `npm run probe`, the complete
  guide directory and signed archive carrier, and first-touch repository instructions.
- owner: `CEAL_COMMANDS` owns effect and exceptional lifecycle;
  `probe-surface.mjs` owns probe admission; the guide and AGENTS own their prose.
- projections: command help/YAML, probe refusal, guide archive/install, agent orientation.
- state scope: one checkout child process, two skill directories, one installed
  release generation, and the repository entrypoint.
- transitions: finite command or until-interrupted service; discover, select,
  invoke, and read back; orient, then descend to an owner document.
- proof boundary: focused tests, archive inspection, inventories, local reproduction.
- unexamined axes: Windows process behavior, signed guide bytes, live Gateway,
  and generic static inference of subprocess semantics across languages.

## Current Gates

- Focused probe and guide contract tests pass on the repaired tree.
- Both skills pass syntax validation and the skill ergonomic inventory reports
  zero heuristic findings; prose review remains judgment-required.
- Installer fixtures prove nested references survive installation and unsafe tar
  entry types are refused before extraction.
- `npm run check:unit` and `npm run check:protocol-dev` pass on the repaired tree;
  ship-facing release paths remain quarantined by the pre-existing B1 divergence.

## Runtime Signals

- runtime source: `render_runtime_summary.py --repo-root . --detail` over the
  repo-declared gitignored timing log. <!-- reproduction-source -->
- runtime hot spots: the current summary reports no standing budget breach.
- coverage gate: `npm run check:unit` passed; release-input paths remain outside
  that development claim while the local Protocol pin and shipment lock diverge.
- evaluator depth: deterministic tests, archive inspection, and bounded process-group reproduction.

## Healthy

- Command effect and process settlement are now separate machine-readable facts.
- Help remains probeable, while a non-help route declared
  `until_interrupted` is refused before any child or temporary HOME exists.
- `AGENTS.md` now orients and links instead of duplicating gate history and rationale.
- The guide is now a real progressively disclosed directory: concise core,
  capability workflow, and private-link handling are separately discoverable.
- Release inventory, native/package builders, manifest, signature workflow, and
  installer derive from one directory and one deterministic `ceal-guide.tar`.
- The audit helper now bounds time/output, kills its process group, and omits
  operands from metrics; both skill packages have zero heuristic findings.

## Weak

- Charness finds nested CLI call sites but cannot classify deadline, lifecycle,
  termination ownership, or output bounds; repo-local declaration is still needed.
- `corca-ai/charness#607` tracks that cross-repo inventory gap.

## Missing

- In-place update from 0.76.1 to the first directory-carrier release is
  intentionally unsupported; that generation requires a bootstrap reinstall.

## Deferred

- Release, push, tag, Gateway write, and publication remain unauthorized.
- Signed/install proof needs Gateway handoff convergence and an approved
  directory release.

## Advisory

- command: `inventory_standing_test_economics.py --repo-root . --summary` found
  nested CLI fanout but emitted no settlement fields. Current Charness `main` at
  `efb6017c` has the same payload shape in `standing_test_economics_lib.py`.
- command: a detached process-group run of
  `node scripts/probe-surface.mjs ceal observe --port 0` remained alive until
  the group was terminated. This is the consumer impact, not a speculative lint rule.
- command: `inventory_entrypoint_docs_ergonomics.py --repo-root . --detail`
  no longer flags `AGENTS.md` after the owner-preserving compression.
- command: `inventory_skill_ergonomics.py --repo-root . --skill-path
  skills/ceal-guide --skill-path skills/ceal-capability-audit --summary` reports
  zero heuristic findings and still requires prose review.
- artifact: `skills/ceal-guide/` prose review finds its trigger narrowly worker-facing; core
  owns selection and sequencing; each directly listed reference deepens one
  selected concept; conditional idempotency and no-auto-open private-source
  safety remain explicit. The audit trigger is exhaustive-only and requires the
  installed guide package rather than a checkout-relative substitute.

## Charness Issue Candidate

Title: Standing-test inventory finds nested CLI fanout but cannot expose subprocess settlement risk

Observed: spawn inventory has no deadline, lifecycle, process-tree, or output-bound
classification. The sanctioned `ceal observe` probe never settled. Current local
Charness `main` has the installed plugin 4.1.0 gap.

Desired acceptance: for each detected subprocess seam, emit conservative fields
for deadline presence, lifecycle (`finite`, `until_interrupted`, or `unknown`),
termination ownership, and output bounding, or route a dedicated inventory into
`quality`. Fixtures distinguish visibly bounded, unbounded, and syntactically
unknown seams without pretending to prove child semantics.

## Delegated Review

- Delegated Review: executed — two rounds covered lifecycle/skill and archive/
  install safety with clean boundary fingerprints. Round 2 reproduced descendant-
  pipe and pre-read file-bound bypasses; cap-required repairs are accepted-unreviewed
  and fixed by direct descendant, FIFO, size, duplicate, and mode falsifiers.
- Slow-gate lenses: fixture-economics, parallel-critical-path, and duplicated-proof
  were reviewed in the prior spawn-economics artifact; this slice adds no broad slow gate.

## Commands Run

- Charness runtime/standing-test/skill/entrypoint inventories and local-main comparison
- bounded `probe-surface.mjs ceal observe --port 0` reproduction and `npm run build:worker`
- focused probe, guide, archive, compose, installer, shell, and skill validation

## Recommended Next Quality Moves

- active verify and commit the direct directory rollout —
  capability_needed=maintainer release sequencing plus source/runtime proof;
  next_center=release carrier and stable bootstrap; transformation=accept one
  update discontinuity; proof_boundary=fresh install with nested references;
  enforcement_posture=release blocker until the signed handoff converges.
- passive because the upstream now owns the tracked gap, follow
  `corca-ai/charness#607`; this repo cannot enforce another product's inventory.

## History

- [Acceptance and lock integrity](2026-08-11-acceptance-lock-integrity.md)
- [Spawn economics](2026-08-11-spawn-economics.md)
- [Earlier quality baseline](history/2026-07-27-quality-review.md)
